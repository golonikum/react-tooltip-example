import { styled } from 'styled-components';

export const StyledTooltipWrap = styled.div`
  position: absolute;
  z-index: 100;
  top: 0;
  left: 0;
  visibility: hidden;
  display: flex;
  box-sizing: border-box;
`;
